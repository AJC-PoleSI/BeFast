---
phase: 02-profils-documents
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/ui/tabs.tsx
  - components/ui/dialog.tsx
  - components/ui/textarea.tsx
  - app/(dashboard)/dashboard/profil/page.tsx
  - app/(dashboard)/dashboard/profil/_components/profile-header.tsx
  - app/(dashboard)/dashboard/profil/_components/avatar-upload.tsx
  - app/(dashboard)/dashboard/profil/_components/profile-info-card.tsx
  - app/(dashboard)/dashboard/profil/_components/sensitive-field-card.tsx
  - app/(dashboard)/dashboard/profil/_components/sensitive-edit-modal.tsx
  - app/(dashboard)/dashboard/profil/_components/document-grid.tsx
  - app/(dashboard)/dashboard/profil/_components/document-slot.tsx
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-05, DOCS-01, DOCS-02, DOCS-03, DOCS-05, DOCS-06]

must_haves:
  truths:
    - "User sees profile page with Informations and Documents tabs"
    - "User can edit basic info and save via PATCH /api/profil"
    - "User sees masked **** for NSS/IBAN with Modifier button opening a modal"
    - "User can upload documents via button and see preview/status"
    - "User can delete documents with confirmation dialog"
    - "Filigrane link opens external site in new tab"
  artifacts:
    - path: "app/(dashboard)/dashboard/profil/page.tsx"
      provides: "Profile page with tabs"
      contains: "Informations"
    - path: "app/(dashboard)/dashboard/profil/_components/sensitive-edit-modal.tsx"
      provides: "NSS/IBAN edit dialog"
      contains: "Dialog"
    - path: "app/(dashboard)/dashboard/profil/_components/document-slot.tsx"
      provides: "Single document slot card"
      contains: "DocumentSlot"
  key_links:
    - from: "app/(dashboard)/dashboard/profil/_components/profile-info-card.tsx"
      to: "/api/profil"
      via: "fetch PATCH"
      pattern: "fetch.*api/profil"
    - from: "app/(dashboard)/dashboard/profil/_components/sensitive-edit-modal.tsx"
      to: "/api/profil/sensitive"
      via: "fetch POST"
      pattern: "api/profil/sensitive"
    - from: "app/(dashboard)/dashboard/profil/_components/document-slot.tsx"
      to: "/api/profil/documents"
      via: "fetch POST for upload"
      pattern: "api/profil/documents"
---

<objective>
Build the complete profile page UI: tab layout, profile info card with edit mode, sensitive field cards with edit modals, avatar upload, and document grid with upload/preview/delete slots.

Purpose: Users can view and manage their profile and documents through a polished French-language interface.
Output: Profile page + 7 custom components consuming the API routes from Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-profils-documents/02-CONTEXT.md
@.planning/phases/02-profils-documents/02-UI-SPEC.md
@.planning/phases/02-profils-documents/02-01-SUMMARY.md

<interfaces>
From app/(dashboard)/dashboard/profil/_lib/schemas.ts (created in Plan 01):
```typescript
export const profileSchema: z.ZodObject<{prenom, nom, portable, promo, adresse, ville, code_postal, pole}>
export type ProfileFormValues = z.infer<typeof profileSchema>
export const sensitiveFieldSchema: z.ZodObject<{field, value, confirmation}>
export const VALID_DOC_TYPES: readonly ["carte_identite", "carte_etudiante", "carte_vitale", "preuve_lydia", "rib"]
export const MAX_FILE_SIZE: number  // 10 Mo
export const ACCEPTED_FILE_TYPES: string[]
export const DOC_TYPE_LABELS: Record<string, string>
```

From types/database.types.ts:
```typescript
export type DocumentType = 'carte_identite' | 'carte_etudiante' | 'carte_vitale' | 'preuve_lydia' | 'rib'
export interface DocumentPersonne { id, personne_id, type, file_path, file_name, file_size, mime_type, created_at, updated_at }
export interface Personne { id, email, prenom, nom, portable, promo, adresse, ville, code_postal, pole, nss_encrypted, iban_encrypted, ... }
export interface PersonneWithRole extends Personne { profils_types: ProfilType | null }
```

From hooks/useUser.ts:
```typescript
export function useUser(): { user, profile, permissions, loading, isAdmin }
```

API Routes (Plan 01):
- PATCH /api/profil — update basic profile
- POST /api/profil/sensitive — encrypt + save NSS/IBAN
- POST /api/profil/avatar — upload avatar
- POST /api/profil/documents — upload document
- GET /api/profil/documents — list user documents
- DELETE /api/profil/documents/[docId] — delete document
- POST /api/profil/documents/signed-url — get 1h signed URL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components + profile page + header + avatar</name>
  <files>components/ui/tabs.tsx, components/ui/dialog.tsx, components/ui/textarea.tsx, app/(dashboard)/dashboard/profil/page.tsx, app/(dashboard)/dashboard/profil/_components/profile-header.tsx, app/(dashboard)/dashboard/profil/_components/avatar-upload.tsx</files>
  <read_first>
    - components/ui/card.tsx
    - components/ui/avatar.tsx
    - components/ui/button.tsx
    - hooks/useUser.ts
    - app/(dashboard)/dashboard/profil/_lib/schemas.ts
    - types/database.types.ts
    - app/globals.css
  </read_first>
  <action>
1. Install shadcn components: `npx shadcn@latest add tabs dialog textarea --yes`

2. Create `app/(dashboard)/dashboard/profil/page.tsx` — "use client" page:
   - Import useUser hook, Tabs/TabsContent/TabsList/TabsTrigger from shadcn
   - Show Skeleton loading state while useUser loads
   - Layout per 02-UI-SPEC: page padding 32px (p-8), title "Mon profil" in Playfair Display 28px font-bold
   - ProfileHeader component (avatar + name)
   - Tabs with "Informations" and "Documents" triggers
   - Active tab: gold #C9A84C bottom border 2px + gold text (use className on TabsTrigger: `data-[state=active]:text-[#C9A84C] data-[state=active]:border-b-2 data-[state=active]:border-[#C9A84C]`)
   - TabsContent "informations": ProfileInfoCard + SensitiveFieldCard
   - TabsContent "documents": DocumentGrid
   - State: documents array fetched from GET /api/profil/documents on mount

3. Create `app/(dashboard)/dashboard/profil/_components/profile-header.tsx`:
   - Props: `{ profile: PersonneWithRole, onAvatarUpdate: (url: string) => void }`
   - Flex row, gap 16px, items-center
   - AvatarUpload component (80px)
   - Name: `{prenom} {nom}` in Heading 20px
   - Email below in muted text 14px

4. Create `app/(dashboard)/dashboard/profil/_components/avatar-upload.tsx`:
   - Props: `{ avatarUrl: string | null, prenom: string | null, nom: string | null, onUpdate: (url: string) => void }`
   - Avatar component 80px (h-20 w-20) with AvatarImage + AvatarFallback (initials, bg-[#C9A84C] text-[#0D1B2A] text-xl font-bold)
   - Hidden file input (accept="image/*")
   - "Changer la photo" link below avatar (text-sm text-[#4A90D9] cursor-pointer)
   - On file select: validate max 2Mo, POST to /api/profil/avatar as FormData, toast success "Photo de profil mise a jour.", call onUpdate with new URL
   - On error: toast "Erreur lors de l'upload. Veuillez reessayer."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && test -f components/ui/tabs.tsx && test -f components/ui/dialog.tsx && grep -q "Mon profil" app/\(dashboard\)/dashboard/profil/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f components/ui/tabs.tsx` succeeds
    - `test -f components/ui/dialog.tsx` succeeds
    - `grep "Mon profil" app/(dashboard)/dashboard/profil/page.tsx` matches
    - `grep "Informations" app/(dashboard)/dashboard/profil/page.tsx` matches
    - `grep "Documents" app/(dashboard)/dashboard/profil/page.tsx` matches
    - `grep "C9A84C" app/(dashboard)/dashboard/profil/page.tsx` matches (gold active tab)
    - `grep "AvatarFallback" app/(dashboard)/dashboard/profil/_components/avatar-upload.tsx` matches
    - `grep "Changer la photo" app/(dashboard)/dashboard/profil/_components/avatar-upload.tsx` matches
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Profile page renders with tabs, avatar with fallback initials, and gold active tab styling. shadcn tabs, dialog, textarea installed.</done>
</task>

<task type="auto">
  <name>Task 2: Info card (edit mode), sensitive card + modal, document grid + slots</name>
  <files>app/(dashboard)/dashboard/profil/_components/profile-info-card.tsx, app/(dashboard)/dashboard/profil/_components/sensitive-field-card.tsx, app/(dashboard)/dashboard/profil/_components/sensitive-edit-modal.tsx, app/(dashboard)/dashboard/profil/_components/document-grid.tsx, app/(dashboard)/dashboard/profil/_components/document-slot.tsx</files>
  <read_first>
    - app/(dashboard)/dashboard/profil/page.tsx
    - app/(dashboard)/dashboard/profil/_lib/schemas.ts
    - components/ui/form.tsx
    - components/ui/input.tsx
    - components/ui/card.tsx
    - components/ui/dialog.tsx
    - components/ui/badge.tsx
    - types/database.types.ts
    - app/globals.css
  </read_first>
  <action>
1. **`profile-info-card.tsx`** (PROF-01, PROF-05):
   - Props: `{ profile: PersonneWithRole, onUpdate: (p: PersonneWithRole) => void, readOnly?: boolean }`
   - Card with title "Informations de base" (Heading 20px) and "Modifier" ghost Button top-right
   - Read mode: display fields as text (grid 2-col lg, 1-col mobile, gap-4)
   - Fields: Prenom, Nom, Portable, Promo, Adresse, Ville, Code postal, Pole (Select with options: "Developpement", "Commercial", "Communication", "Tresorerie", "Presidence", "Secretariat", "Qualite", "RH", "" for none)
   - Edit mode: react-hook-form with zodResolver(profileSchema), fields become Input/Select components
   - "Enregistrer" button (bg-[#C9A84C] text-[#0D1B2A] hover:bg-[#C9A84C]/90) + "Annuler" ghost button at bottom
   - On submit: fetch PATCH /api/profil with JSON body, toast "Profil mis a jour avec succes." on success, toggle back to read mode
   - If readOnly prop true: no "Modifier" button (for admin view of sensitive section)
   - Empty state for fields: show "Non renseigne" in muted text

2. **`sensitive-field-card.tsx`** (PROF-02):
   - Props: `{ profile: PersonneWithRole, isOwnProfile: boolean }`
   - Card with title "Donnees sensibles" (Heading 20px)
   - Two rows: NSS and IBAN
   - Each row: label, masked value "****" (or "Non renseigne" if null), "Modifier" ghost Button
   - "Modifier" buttons only shown if `isOwnProfile` is true (admin cannot edit others' sensitive data per CONTEXT.md)
   - Each "Modifier" opens SensitiveEditModal

3. **`sensitive-edit-modal.tsx`** (PROF-03):
   - Props: `{ open: boolean, onOpenChange, field: "nss" | "iban", onSuccess: () => void }`
   - Dialog with title "Modifier le numero de securite sociale" or "Modifier l'IBAN"
   - react-hook-form with zodResolver(sensitiveFieldSchema)
   - Two Input fields: "Nouveau NSS"/"Nouvel IBAN" and "Confirmer le NSS"/"Confirmer l'IBAN"
   - On submit: POST /api/profil/sensitive with { field, value, confirmation }
   - Success: close dialog, toast "Enregistre avec succes.", call onSuccess
   - Error: show inline error + toast
   - "Enregistrer" gold CTA + "Annuler" ghost button

4. **`document-grid.tsx`** (DOCS-01, DOCS-06):
   - Props: `{ documents: DocumentPersonne[], onDocumentsChange: () => void, readOnly?: boolean }`
   - Title "Vos documents personnels" (Heading 20px)
   - "Ajouter un filigrane" link top-right: `<a href="https://filigrane.beta.gouv.fr" target="_blank" rel="noopener noreferrer">` styled text-[#4A90D9] text-sm with ExternalLink lucide icon
   - Grid 2-col lg, 1-col mobile, gap-4
   - Map over VALID_DOC_TYPES, render DocumentSlot for each, passing matching document from documents array (or null if missing)

5. **`document-slot.tsx`** (DOCS-01, DOCS-02, DOCS-03, DOCS-05):
   - Props: `{ docType: DocumentType, document: DocumentPersonne | null, onDocumentsChange: () => void, readOnly?: boolean }`
   - Card min-h-[120px] p-4
   - Header: DOC_TYPE_LABELS[docType] bold + Badge ("Uploade" bg-green-100 text-green-800 if document, "Manquant" bg-orange-100 text-orange-800 if null)
   - **If document exists (uploaded)**:
     - Preview: if mime_type starts with "image/", show img tag with signed URL (fetch on mount from POST /api/profil/documents/signed-url with { filePath: document.file_path }), max-h-[160px] object-contain. If PDF, show FileText lucide icon + file_name.
     - "Telecharger" link (text-[#4A90D9]) — fetch signed URL and window.open
     - "Supprimer" destructive ghost button (text-red-600) — opens Dialog confirmation "Supprimer ce document ?" body "Cette action est irreversible." with "Supprimer" destructive button + "Annuler"
     - On delete confirm: DELETE /api/profil/documents/{document.id}, toast "Document supprime.", call onDocumentsChange
   - **If no document (missing)**:
     - "Choisir un fichier" outline Button triggering hidden file input (accept="image/*,application/pdf")
     - Note below: "10 Mo maximum — formats acceptes : image ou PDF" in 14px muted text
     - On file select: validate size (MAX_FILE_SIZE) and type (ACCEPTED_FILE_TYPES), show Skeleton during upload
     - POST /api/profil/documents as FormData (file + docType), toast "Document uploade avec succes.", call onDocumentsChange
     - On size error: toast "Le fichier depasse la taille maximale de 10 Mo."
     - On type error: toast "Format non accepte. Utilisez une image ou un PDF."
   - If readOnly: no upload button, no delete button (admin can only view)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && grep -q "Informations de base" app/\(dashboard\)/dashboard/profil/_components/profile-info-card.tsx && grep -q "filigrane.beta.gouv.fr" app/\(dashboard\)/dashboard/profil/_components/document-grid.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Informations de base" app/(dashboard)/dashboard/profil/_components/profile-info-card.tsx` matches
    - `grep "Donnees sensibles" app/(dashboard)/dashboard/profil/_components/sensitive-field-card.tsx` matches
    - `grep "api/profil/sensitive" app/(dashboard)/dashboard/profil/_components/sensitive-edit-modal.tsx` matches
    - `grep "filigrane.beta.gouv.fr" app/(dashboard)/dashboard/profil/_components/document-grid.tsx` matches
    - `grep "_blank" app/(dashboard)/dashboard/profil/_components/document-grid.tsx` matches
    - `grep "Supprimer ce document" app/(dashboard)/dashboard/profil/_components/document-slot.tsx` matches
    - `grep "createSignedUrl\|signed-url" app/(dashboard)/dashboard/profil/_components/document-slot.tsx` matches
    - `grep "Uploade\|Manquant" app/(dashboard)/dashboard/profil/_components/document-slot.tsx` matches
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>All 5 components render: info card with read/edit mode, sensitive card with masked display, edit modal with double-entry, document grid with filigrane link, document slots with upload/preview/delete.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` succeeds
- Profile page renders at /dashboard/profil with both tabs
- All French copy matches 02-UI-SPEC copywriting contract
</verification>

<success_criteria>
- Profile page has Informations and Documents tabs with gold active indicator
- Avatar shows initials fallback with gold background
- Basic info editable with Enregistrer/Annuler flow
- NSS/IBAN show **** with Modifier opening Dialog modal
- 5 document slots show uploaded/missing status with correct badges
- Filigrane link opens in new tab
- All components consume API routes from Plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/02-profils-documents/02-02-SUMMARY.md`
</output>
