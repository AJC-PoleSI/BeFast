---
phase: 02-profils-documents
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/(dashboard)/dashboard/profil/[userId]/page.tsx
autonomous: false
requirements: [PROF-06, SEC-01, SEC-02, SEC-03]

must_haves:
  truths:
    - "Admin can view any member's profile via /dashboard/profil/[userId]"
    - "Admin can edit basic info of another member"
    - "Admin cannot edit NSS/IBAN of another member"
    - "Non-admin accessing /dashboard/profil/[userId] is redirected"
  artifacts:
    - path: "app/(dashboard)/dashboard/profil/[userId]/page.tsx"
      provides: "Admin profile view"
      contains: "Profil de"
  key_links:
    - from: "app/(dashboard)/dashboard/profil/[userId]/page.tsx"
      to: "/api/profil"
      via: "PATCH with targetUserId query param"
      pattern: "targetUserId"
---

<objective>
Create the admin profile view page and verify all security requirements (RLS, server-only guards, private bucket).

Purpose: Admins can manage member profiles. Security posture is verified.
Output: Admin profile page + security verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-profils-documents/02-CONTEXT.md
@.planning/phases/02-profils-documents/02-UI-SPEC.md
@.planning/phases/02-profils-documents/02-01-SUMMARY.md
@.planning/phases/02-profils-documents/02-02-SUMMARY.md

<interfaces>
From app/(dashboard)/dashboard/profil/page.tsx (Plan 02):
- Uses ProfileHeader, ProfileInfoCard, SensitiveFieldCard, DocumentGrid components
- Tab layout with Informations/Documents

From app/api/profil/route.ts (Plan 01):
- PATCH /api/profil?targetUserId={id} — admin can update other user's basic info

From app/api/profil/documents/route.ts (Plan 01):
- GET /api/profil/documents?targetUserId={id} — admin can list other user's docs

From lib/supabase/server.ts:
```typescript
export function createClient()  // SSR cookie-based
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin profile view /dashboard/profil/[userId]</name>
  <files>app/(dashboard)/dashboard/profil/[userId]/page.tsx</files>
  <read_first>
    - app/(dashboard)/dashboard/profil/page.tsx
    - app/(dashboard)/dashboard/profil/_components/profile-header.tsx
    - app/(dashboard)/dashboard/profil/_components/profile-info-card.tsx
    - app/(dashboard)/dashboard/profil/_components/sensitive-field-card.tsx
    - app/(dashboard)/dashboard/profil/_components/document-grid.tsx
    - lib/supabase/server.ts
    - types/database.types.ts
  </read_first>
  <action>
Create `app/(dashboard)/dashboard/profil/[userId]/page.tsx` — "use client" page for admin viewing another member's profile (PROF-06):

1. Import useUser hook, check if isAdmin. If not admin AND userId !== own user.id, redirect to /dashboard (use `router.push('/dashboard')` + return null).
2. Fetch target user profile: GET from supabase `personnes` table with `select("*, profils_types(*)")` where id = params.userId (use client-side supabase since RLS admin policy allows it).
3. Fetch target user documents: GET /api/profil/documents?targetUserId={userId}
4. Page title: "Profil de {prenom} {nom}" instead of "Mon profil" (per 02-UI-SPEC).
5. Reuse same components as own profile page:
   - ProfileHeader (with target profile data)
   - Tabs Informations/Documents
   - ProfileInfoCard — pass `onUpdate` that calls PATCH /api/profil?targetUserId={userId}. Admin CAN edit basic info.
   - SensitiveFieldCard — pass `isOwnProfile={false}` so "Modifier" buttons are hidden (admin cannot edit others' sensitive data per CONTEXT.md)
   - DocumentGrid — pass `readOnly={true}` so admin can view but not upload/delete
6. Show Skeleton while loading, handle 404 if user not found.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && grep -q "Profil de" app/\(dashboard\)/dashboard/profil/\[userId\]/page.tsx && grep -q "isOwnProfile.*false\|isOwnProfile={false}" app/\(dashboard\)/dashboard/profil/\[userId\]/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Profil de" app/(dashboard)/dashboard/profil/[userId]/page.tsx` matches
    - `grep "isOwnProfile" app/(dashboard)/dashboard/profil/[userId]/page.tsx` matches with false value
    - `grep "readOnly" app/(dashboard)/dashboard/profil/[userId]/page.tsx` matches with true value
    - `grep "targetUserId" app/(dashboard)/dashboard/profil/[userId]/page.tsx` matches
    - `grep "isAdmin" app/(dashboard)/dashboard/profil/[userId]/page.tsx` matches (admin check)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Admin profile page renders at /dashboard/profil/[userId] with editable basic info, read-only sensitive fields (no Modifier), and read-only documents. Non-admins are redirected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full profile flow + security</name>
  <what-built>Complete profile management: own profile (edit basic info, modify NSS/IBAN via modal, upload/delete documents, avatar upload) and admin view of member profiles.</what-built>
  <how-to-verify>
    1. Visit /dashboard/profil — verify "Mon profil" title, avatar with initials, Informations/Documents tabs with gold active indicator
    2. Tab Informations: click "Modifier", edit a field, click "Enregistrer" — verify toast "Profil mis a jour avec succes."
    3. Sensitive fields: verify **** display, click "Modifier" on NSS — verify Dialog opens with double-entry fields
    4. Tab Documents: verify 5 slots with "Manquant" badges, verify "Ajouter un filigrane" link opens filigrane.beta.gouv.fr
    5. Upload a document — verify "Uploade" badge appears with preview
    6. Delete the document — verify confirmation dialog, then "Manquant" badge returns
    7. Security: verify `grep -r "NEXT_PUBLIC_SUPABASE_SERVICE_ROLE\|SUPABASE_SERVICE_ROLE_KEY" app/ components/ hooks/` returns NO matches outside app/api/ and lib/ (SEC-02)
    8. If admin account available: visit /dashboard/profil/{otherUserId} — verify basic info is editable but NSS/IBAN has no "Modifier" buttons and documents are read-only
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Admin page at /dashboard/profil/[userId] compiles and renders
- `grep -r "service_role\|SUPABASE_SERVICE_ROLE_KEY" app/ components/ hooks/` returns NO matches (only in lib/supabase/admin.ts)
- All 15 requirements (PROF-01 to PROF-06, DOCS-01 to DOCS-06, SEC-01 to SEC-03) addressed across Plans 01-03
</verification>

<success_criteria>
- Admin can view member profiles via direct URL
- Admin can edit basic info but not sensitive fields of others
- Non-admin redirected away from [userId] route
- All security requirements verified: RLS migration ready, server-only guards on all API routes, private bucket configured
</success_criteria>

<output>
After completion, create `.planning/phases/02-profils-documents/02-03-SUMMARY.md`
</output>
