---
phase: 01-fondation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - middleware.ts
  - app/(auth)/layout.tsx
  - app/(auth)/login/page.tsx
  - app/(auth)/inscription/page.tsx
  - app/(auth)/mot-de-passe-oublie/page.tsx
  - app/(dashboard)/layout.tsx
  - app/(dashboard)/dashboard/page.tsx
  - app/(dashboard)/attente/page.tsx
  - components/layout/Sidebar.tsx
  - components/layout/Header.tsx
  - components/layout/RoleGuard.tsx
  - components/ui/status-badge.tsx
  - hooks/useUser.ts
  - types/database.types.ts
  - lib/actions/auth.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-07
  - ROLE-01
  - ROLE-02
  - ROLE-03
  - ROLE-04
  - ROLE-05
  - ROLE-06
  - UX-04
  - UX-05
  - UX-06
  - SEC-04
must_haves:
  truths:
    - "Un utilisateur peut creer un compte avec email et mot de passe depuis /inscription"
    - "Un utilisateur peut se connecter depuis /login et est redirige vers /dashboard"
    - "Un compte sans role assigne voit l'ecran /attente apres connexion"
    - "Les routes /dashboard/* redirigent vers /login si non authentifie"
    - "La sidebar affiche uniquement les items accessibles selon les permissions du role"
    - "Le composant RoleGuard bloque l'acces aux pages non autorisees"
    - "L'interface auth est 100% en francais avec la palette BeFast"
  artifacts:
    - path: "middleware.ts"
      provides: "Protection routes dashboard + refresh session"
      contains: "getUser"
    - path: "app/(auth)/login/page.tsx"
      provides: "Page connexion email/mot de passe"
      contains: "signInWithPassword"
    - path: "app/(auth)/inscription/page.tsx"
      provides: "Page inscription"
      contains: "signUp"
    - path: "app/(dashboard)/attente/page.tsx"
      provides: "Ecran attente role"
      contains: "en attente"
    - path: "components/layout/Sidebar.tsx"
      provides: "Sidebar avec filtrage par permissions"
      contains: "permissions"
    - path: "components/layout/RoleGuard.tsx"
      provides: "Guard client-side par permission"
      contains: "permission"
    - path: "hooks/useUser.ts"
      provides: "Hook profil + permissions utilisateur"
      exports: ["useUser"]
    - path: "types/database.types.ts"
      provides: "Types TypeScript pour DB"
      contains: "Personne"
  key_links:
    - from: "middleware.ts"
      to: "@supabase/ssr"
      via: "createServerClient + getUser()"
      pattern: "supabase\\.auth\\.getUser"
    - from: "app/(auth)/login/page.tsx"
      to: "lib/supabase/client.ts"
      via: "createClient browser"
      pattern: "signInWithPassword"
    - from: "components/layout/Sidebar.tsx"
      to: "hooks/useUser.ts"
      via: "permissions prop from layout"
      pattern: "permissions"
    - from: "app/(dashboard)/layout.tsx"
      to: "lib/supabase/server.ts"
      via: "server-side user fetch"
      pattern: "createClient"
---

<objective>
Implementer l'authentification complete (inscription, connexion, mot de passe oublie), le middleware de protection des routes, le layout dashboard avec sidebar filtree par role, le composant RoleGuard, et l'ecran d'attente pour les comptes sans role.

Purpose: Permettre aux utilisateurs de s'authentifier et d'acceder a une interface personnalisee selon leur role — c'est le coeur fonctionnel de la Phase 1.
Output: Pages auth fonctionnelles, middleware protecteur, sidebar avec permissions, types TypeScript, hook useUser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/TECH-STACK.md
@.planning/phases/01-fondation/01-UI-SPEC.md
@.planning/phases/01-fondation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — utilities the executor will import -->

From lib/supabase/server.ts:
```typescript
export function createClient(): SupabaseClient
// Uses @supabase/ssr createServerClient with cookies()
```

From lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient
// Uses @supabase/ssr createBrowserClient
```

From lib/supabase/admin.ts:
```typescript
export function createAdminClient(): SupabaseClient
// Uses service_role key, guarded by 'server-only'
```

SQL schema (from 001_init_schema.sql):
```sql
-- profils_types: id UUID, nom TEXT, slug TEXT, permissions JSONB, est_defaut BOOLEAN
-- personnes: id UUID (refs auth.users), email TEXT, prenom TEXT, nom TEXT, profil_type_id UUID (refs profils_types)
-- 4 default roles: membre_agc, ancien_membre_agc, intervenant, administrateur
-- permissions JSONB keys: dashboard, profil, missions, etudes, prospection, statistiques, administration, documents
```

From 003_functions.sql:
```sql
-- custom_access_token_hook embeds app_role (slug) in JWT claims
-- get_my_role() reads request.jwt.claims->>'app_role'
-- has_permission(page_name text) checks permissions JSONB via join
```

shadcn/ui components installed: button, input, label, form, card, separator, avatar, skeleton, sonner, badge
Tailwind aliases: navy="#0D1B2A", gold="#C9A84C", ivory="#F5F0E8", blue="#4A90D9"
Font CSS vars: --font-playfair, --font-dm-sans
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, hook useUser, middleware, et server actions auth</name>
  <files>
    types/database.types.ts, hooks/useUser.ts, middleware.ts, lib/actions/auth.ts
  </files>
  <read_first>
    .planning/research/TECH-STACK.md (section 1 — middleware pattern exact)
    .planning/phases/01-fondation/01-01-SUMMARY.md (what Plan 01 created)
    lib/supabase/server.ts
    lib/supabase/client.ts
  </read_first>
  <action>
1. **types/database.types.ts** — Types TypeScript pour le schema BeFast:

```typescript
export type PermissionKey =
  | "dashboard"
  | "profil"
  | "missions"
  | "etudes"
  | "prospection"
  | "statistiques"
  | "administration"
  | "documents"

export type Permissions = Record<PermissionKey, boolean>

export interface ProfilType {
  id: string
  nom: string
  slug: string
  permissions: Permissions
  est_defaut: boolean
  created_at: string
  updated_at: string
}

export interface Personne {
  id: string
  email: string
  prenom: string | null
  nom: string | null
  portable: string | null
  promo: string | null
  adresse: string | null
  ville: string | null
  code_postal: string | null
  pole: string | null
  nss_encrypted: string | null
  iban_encrypted: string | null
  encryption_key_version: number
  profil_type_id: string | null
  actif: boolean
  created_at: string
  updated_at: string
}

export interface PersonneWithRole extends Personne {
  profils_types: ProfilType | null
}
```

2. **hooks/useUser.ts** — Client-side hook (ROLE-02):

```typescript
"use client"

import { createClient } from "@/lib/supabase/client"
import { useEffect, useState } from "react"
import type { PersonneWithRole, Permissions } from "@/types/database.types"
import type { User } from "@supabase/supabase-js"

interface UseUserReturn {
  user: User | null
  profile: PersonneWithRole | null
  permissions: Permissions | null
  loading: boolean
  isAdmin: boolean
}

const emptyPermissions: Permissions = {
  dashboard: false, profil: false, missions: false, etudes: false,
  prospection: false, statistiques: false, administration: false, documents: false,
}

export function useUser(): UseUserReturn {
  const [user, setUser] = useState<User | null>(null)
  const [profile, setProfile] = useState<PersonneWithRole | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    const supabase = createClient()

    async function load() {
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) { setLoading(false); return }
      setUser(user)

      const { data } = await supabase
        .from("personnes")
        .select("*, profils_types(*)")
        .eq("id", user.id)
        .single()

      if (data) setProfile(data as PersonneWithRole)
      setLoading(false)
    }

    load()

    const { data: { subscription } } = supabase.auth.onAuthStateChange(() => {
      load()
    })

    return () => subscription.unsubscribe()
  }, [])

  const permissions = profile?.profils_types?.permissions ?? null
  const isAdmin = profile?.profils_types?.slug === "administrateur"

  return { user, profile, permissions, loading, isAdmin }
}
```

3. **middleware.ts** — Copier EXACTEMENT le pattern de TECH-STACK.md section 1 (SEC-04, AUTH-07). Points critiques:
   - Creer supabaseResponse = NextResponse.next({ request })
   - Creer le client avec createServerClient de @supabase/ssr
   - Appeler getUser() IMMEDIATEMENT apres creation du client — aucune logique entre les deux
   - Si !user ET path ne commence pas par /login, /inscription, /mot-de-passe-oublie, /auth/callback: redirect vers /login
   - Si user ET path est /login ou /inscription: redirect vers /dashboard
   - TOUJOURS retourner supabaseResponse, JAMAIS NextResponse.next()
   - Matcher: `/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)`

```typescript
import { createServerClient } from "@supabase/ssr"
import { NextResponse, type NextRequest } from "next/server"

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          )
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  const { data: { user } } = await supabase.auth.getUser()

  const publicPaths = ["/login", "/inscription", "/mot-de-passe-oublie", "/auth/callback"]
  const isPublicPath = publicPaths.some((p) => request.nextUrl.pathname.startsWith(p))

  if (!user && !isPublicPath) {
    const url = request.nextUrl.clone()
    url.pathname = "/login"
    return NextResponse.redirect(url)
  }

  if (user && (request.nextUrl.pathname === "/login" || request.nextUrl.pathname === "/inscription")) {
    const url = request.nextUrl.clone()
    url.pathname = "/dashboard"
    return NextResponse.redirect(url)
  }

  return supabaseResponse
}

export const config = {
  matcher: [
    "/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
  ],
}
```

4. **lib/actions/auth.ts** — Server actions pour login/signup/logout/reset:

```typescript
"use server"

import { createClient } from "@/lib/supabase/server"
import { redirect } from "next/navigation"

export async function signIn(formData: FormData) {
  const supabase = createClient()
  const { error } = await supabase.auth.signInWithPassword({
    email: formData.get("email") as string,
    password: formData.get("password") as string,
  })
  if (error) return { error: "Identifiants incorrects. Vérifiez votre email et mot de passe." }
  redirect("/dashboard")
}

export async function signUp(formData: FormData) {
  const supabase = createClient()
  const password = formData.get("password") as string
  const confirmPassword = formData.get("confirmPassword") as string

  if (password !== confirmPassword) {
    return { error: "Les mots de passe ne correspondent pas." }
  }
  if (password.length < 8) {
    return { error: "Le mot de passe doit contenir au moins 8 caractères." }
  }

  const { error } = await supabase.auth.signUp({
    email: formData.get("email") as string,
    password,
    options: {
      data: {
        prenom: formData.get("prenom") as string,
        nom: formData.get("nom") as string,
      },
    },
  })

  if (error) {
    if (error.message.includes("already registered")) {
      return { error: "Un compte existe déjà avec cette adresse email." }
    }
    return { error: "Une erreur est survenue. Veuillez réessayer." }
  }

  redirect("/attente")
}

export async function signOut() {
  const supabase = createClient()
  await supabase.auth.signOut()
  redirect("/login")
}

export async function resetPassword(formData: FormData) {
  const supabase = createClient()
  const { error } = await supabase.auth.resetPasswordForEmail(
    formData.get("email") as string,
    { redirectTo: `${process.env.NEXT_PUBLIC_SUPABASE_URL ? "" : "http://localhost:3000"}/auth/callback?next=/dashboard` }
  )
  if (error) return { error: "Une erreur est survenue. Veuillez réessayer." }
  return { success: "Un email de réinitialisation a été envoyé." }
}
```
  </action>
  <verify>
    <automated>cd /Users/felixpitz/Desktop/Befast && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PermissionKey" types/database.types.ts
    - grep -q "Personne" types/database.types.ts
    - grep -q "ProfilType" types/database.types.ts
    - grep -q "PersonneWithRole" types/database.types.ts
    - grep -q "export function useUser" hooks/useUser.ts
    - grep -q "isAdmin" hooks/useUser.ts
    - grep -q "profils_types" hooks/useUser.ts
    - grep -q "getUser" middleware.ts
    - grep -q "supabaseResponse" middleware.ts
    - grep -q "createServerClient" middleware.ts
    - grep -q "/login" middleware.ts
    - grep -q "/inscription" middleware.ts
    - grep -q "/mot-de-passe-oublie" middleware.ts
    - grep -q "signInWithPassword" lib/actions/auth.ts
    - grep -q "signUp" lib/actions/auth.ts
    - grep -q "signOut" lib/actions/auth.ts
    - grep -q "resetPasswordForEmail" lib/actions/auth.ts
    - grep -q "Identifiants incorrects" lib/actions/auth.ts
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Types DB definis, hook useUser retourne profil+permissions+isAdmin, middleware protege les routes dashboard avec getUser(), server actions auth en francais.</done>
</task>

<task type="auto">
  <name>Task 2: Pages auth (login, inscription, mot-de-passe-oublie, attente) + layout auth</name>
  <files>
    app/(auth)/layout.tsx, app/(auth)/login/page.tsx, app/(auth)/inscription/page.tsx,
    app/(auth)/mot-de-passe-oublie/page.tsx, app/(dashboard)/attente/page.tsx
  </files>
  <read_first>
    .planning/phases/01-fondation/01-UI-SPEC.md (page layouts, copy contract, colors)
    lib/actions/auth.ts (server actions from Task 1)
    types/database.types.ts
  </read_first>
  <action>
1. **app/(auth)/layout.tsx** — Layout auth: fond navy, centre le contenu:

```tsx
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen bg-navy flex items-center justify-center p-4">
      {children}
    </div>
  )
}
```

2. **app/(auth)/login/page.tsx** (AUTH-04, UX-01) — Page connexion:
   - Card off-white max-w-[400px] rounded-lg shadow-md p-12 (48px = 2xl)
   - Titre "Connexion" en Playfair Display 28px font-bold (classe font-heading text-[28px])
   - Champs: Email (type="email", name="email") + Mot de passe (type="password", name="password")
   - Labels en francais: "Email", "Mot de passe"
   - CTA: "Se connecter" — w-full h-11 bg-gold text-navy font-bold rounded-md
   - Liens: "Mot de passe oublie ?" → /mot-de-passe-oublie (text-blue text-sm)
   - Lien: "Pas encore de compte ? S'inscrire" → /inscription (text-blue text-sm)
   - Utilise un formulaire HTML classique avec `action` qui appelle signIn server action
   - Affiche les erreurs via state (useActionState ou useState + startTransition)
   - Erreurs affichees via toast Sonner (position top-right, 5000ms) — per UI-SPEC interaction contract
   - Logo "BeFast" en haut de la card: font-heading text-[28px] font-bold, avec "BeFast" en text-gold

3. **app/(auth)/inscription/page.tsx** (AUTH-01) — Page inscription:
   - Meme card layout que login
   - Titre: "Creer un compte" (Playfair 28px)
   - Champs: Prenom, Nom, Email, Mot de passe, Confirmation mot de passe
   - Names: prenom, nom, email, password, confirmPassword
   - CTA: "Creer mon compte" — gold, full-width
   - Note sous le CTA en text-sm text-muted-foreground: "Votre compte sera active apres validation par un administrateur."
   - Lien: "Deja un compte ? Se connecter" → /login
   - Appelle signUp server action

4. **app/(auth)/mot-de-passe-oublie/page.tsx** (AUTH-05) — Reset password:
   - Meme card layout
   - Titre: "Mot de passe oublie" (Playfair 20px heading)
   - Champ: Email
   - CTA: "Envoyer le lien" — gold, full-width
   - Message succes: "Un email de reinitialisation a ete envoye." (toast Sonner)
   - Lien: "Retour a la connexion" → /login
   - Appelle resetPassword server action

5. **app/(dashboard)/attente/page.tsx** (AUTH-02, AUTH-03) — Ecran attente:
   - ATTENTION: cette page est dans le route group (dashboard), pas (auth), car l'utilisateur est authentifie
   - Meme fond navy + card centree que les pages auth (reproduire le layout manuellement ou extraire un wrapper)
   - Icone Clock de lucide-react: taille 48px, couleur gold (#C9A84C)
   - Heading: "Compte en attente de validation" — font-heading text-[20px] font-bold
   - Body: "Votre compte a ete cree. Un administrateur vous assignera un role prochainement. Vous recevrez un acces des que votre profil sera active." — DM Sans 16px, text-muted-foreground
   - Bouton "Se deconnecter" — ghost button, text-blue, 14px
   - Le bouton appelle signOut server action
  </action>
  <verify>
    <automated>cd /Users/felixpitz/Desktop/Befast && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Se connecter" app/\(auth\)/login/page.tsx
    - grep -q "signInWithPassword\|signIn" app/\(auth\)/login/page.tsx
    - grep -q "Mot de passe" app/\(auth\)/login/page.tsx
    - grep -q "bg-navy" app/\(auth\)/layout.tsx
    - grep -q "Creer mon compte" app/\(auth\)/inscription/page.tsx
    - grep -q "signUp" app/\(auth\)/inscription/page.tsx
    - grep -q "prenom" app/\(auth\)/inscription/page.tsx
    - grep -q "validation par un administrateur" app/\(auth\)/inscription/page.tsx
    - grep -q "resetPasswordForEmail\|resetPassword" app/\(auth\)/mot-de-passe-oublie/page.tsx
    - grep -q "en attente" app/\(dashboard\)/attente/page.tsx
    - grep -q "Clock" app/\(dashboard\)/attente/page.tsx
    - grep -q "signOut\|Se déconnecter\|Se deconnecter" app/\(dashboard\)/attente/page.tsx
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Les 4 pages auth sont fonctionnelles avec le design BeFast, toute la copy est en francais, l'ecran d'attente affiche le message avec icone gold et bouton deconnexion.</done>
</task>

<task type="auto">
  <name>Task 3: Layout dashboard, Sidebar, Header, RoleGuard, StatusBadge</name>
  <files>
    app/(dashboard)/layout.tsx, app/(dashboard)/dashboard/page.tsx,
    components/layout/Sidebar.tsx, components/layout/Header.tsx,
    components/layout/RoleGuard.tsx, components/ui/status-badge.tsx
  </files>
  <read_first>
    .planning/phases/01-fondation/01-UI-SPEC.md (sidebar layout, nav items, responsive)
    hooks/useUser.ts
    types/database.types.ts
    lib/supabase/server.ts
  </read_first>
  <action>
1. **types — NavItem interface** (ajouter dans types/database.types.ts ou creer inline):

```typescript
export interface NavItem {
  label: string
  href: string
  icon: string // Lucide icon name
  permission: PermissionKey
}
```

Navigation items (ordre exact):
```typescript
const NAV_ITEMS: NavItem[] = [
  { label: "Tableau de bord", href: "/dashboard", icon: "LayoutDashboard", permission: "dashboard" },
  { label: "Mon Profil", href: "/profil", icon: "User", permission: "profil" },
  { label: "Missions", href: "/missions", icon: "Briefcase", permission: "missions" },
  { label: "Mes Documents", href: "/documents", icon: "FileText", permission: "documents" },
  { label: "Études", href: "/etudes", icon: "GraduationCap", permission: "etudes" },
  { label: "Prospection", href: "/prospection", icon: "TrendingUp", permission: "prospection" },
  { label: "Membres", href: "/membres", icon: "Users", permission: "administration" },
  { label: "Statistiques", href: "/statistiques", icon: "BarChart3", permission: "statistiques" },
  { label: "Administration", href: "/administration", icon: "Settings", permission: "administration" },
]
```

2. **components/layout/Sidebar.tsx** (UX-04, ROLE-05, UX-06):
   - Props: `{ permissions: Permissions | null, userName: string | null }`
   - Background: bg-navy (= bg-[#0D1B2A] via tailwind alias)
   - Width: w-[240px] fixed, height: h-screen, overflow-y-auto
   - Logo en haut: "BeFast" en font-heading text-gold text-xl font-bold, padding p-6
   - Separator sous le logo (border-white/10)
   - Nav items: filtre par permissions — si permissions est null ou permissions[item.permission] !== true, NE PAS rendre l'item (absent du DOM, pas display:none — per UI-SPEC)
   - Item default: text-ivory/70, h-11, px-4, rounded-md, flex items-center gap-3
   - Item hover: hover:bg-white/5 hover:text-ivory
   - Item active (usePathname() match): bg-gold/20 text-gold, icone en text-gold, font-medium
   - Icones: importer dynamiquement depuis lucide-react (LayoutDashboard, User, Briefcase, FileText, GraduationCap, TrendingUp, Users, BarChart3, Settings)
   - **Responsive (UX-06):** Sur mobile (<lg, <1024px) la sidebar est hidden par defaut. Un state `open` controle l'affichage en overlay (fixed inset-0 z-50 avec backdrop bg-black/50). Le hamburger toggle est dans le Header.
   - Exporter aussi un composant `SidebarMobileToggle` (bouton Menu icon) pour le Header

3. **components/layout/Header.tsx**:
   - Props: `{ userName: string | null, onMenuToggle: () => void }`
   - Height: h-14, bg-white, border-b border-border
   - Cote gauche: bouton hamburger (Menu icon, lucide) visible seulement sur mobile (lg:hidden), appelle onMenuToggle
   - Cote droit: Nom utilisateur (text-sm text-foreground) + bouton logout (LogOut icon, lucide)
   - Bouton logout: `aria-label="Se déconnecter"` (obligatoire — bouton icon-only per UI-SPEC)
   - Le logout appelle signOut server action (import de lib/actions/auth.ts)

4. **app/(dashboard)/layout.tsx** (UX-04, ROLE-05):
   - C'est un SERVER component qui fetch le profil + permissions
   - Fetch user via `createClient()` (server) → getUser()
   - Si !user → redirect("/login")
   - Fetch personne avec profils_types: `supabase.from("personnes").select("*, profils_types(*)").eq("id", user.id).single()`
   - Si personne n'a pas de profil_type_id (pas de role assigne) → redirect("/attente")
   - EXCEPTION: si le pathname est /attente, ne pas redirect (sinon boucle infinie). Utiliser une approche ou la page /attente est exclue de ce layout, ou gerer via un check conditionnel.
   - Passe permissions et userName au Sidebar et Header via un client wrapper component
   - Layout: flex horizontal — Sidebar a gauche (hidden sur mobile), contenu a droite (flex-1)
   - Le contenu a un padding p-8 (32px = xl)

5. **app/(dashboard)/dashboard/page.tsx**:
   - Page placeholder simple
   - Heading: "Tableau de bord" en font-heading text-[20px] font-bold
   - Paragraphe: "Bonjour, {prenom}" — recupere via le hook useUser ou passe depuis le layout
   - Card skeleton ou message "Bienvenue sur BeFast"

6. **components/layout/RoleGuard.tsx** (ROLE-06):
   - Client component
   - Props: `{ permission: PermissionKey, children: React.ReactNode }`
   - Utilise useUser() hook pour obtenir les permissions
   - Si loading: afficher Skeleton
   - Si permissions null ou permissions[permission] !== true: afficher un ecran "Acces non autorise" avec message "Vous n'avez pas les permissions pour acceder a cette page." (per UI-SPEC copy contract)
   - Sinon: rendre children

7. **components/ui/status-badge.tsx** (UX-05):
   - Composant base sur shadcn Badge
   - Props: `{ status: "ouverte" | "en_cours" | "terminee" | "annulee" | "en_attente", children?: React.ReactNode }`
   - Mapping couleurs (per UI-SPEC):
     - ouverte: bg-green-100 text-green-800
     - en_cours: bg-blue-100 text-blue-800
     - terminee: bg-gray-100 text-gray-700
     - annulee: bg-red-100 text-red-700
     - en_attente: bg-orange-100 text-orange-800
   - Affiche le label francais: Ouverte, En cours, Terminee, Annulee, En attente
  </action>
  <verify>
    <automated>cd /Users/felixpitz/Desktop/Befast && npx tsc --noEmit 2>&1 | tail -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "permissions" components/layout/Sidebar.tsx
    - grep -q "usePathname" components/layout/Sidebar.tsx
    - grep -q "bg-navy\|#0D1B2A" components/layout/Sidebar.tsx
    - grep -q "text-gold\|#C9A84C" components/layout/Sidebar.tsx
    - grep -q "lg:hidden" components/layout/Sidebar.tsx || grep -q "lg:hidden" components/layout/Header.tsx
    - grep -q 'aria-label="Se déconnecter"' components/layout/Header.tsx
    - grep -q "LogOut\|log-out" components/layout/Header.tsx
    - grep -q "signOut" components/layout/Header.tsx
    - grep -q "getUser" app/\(dashboard\)/layout.tsx
    - grep -q "profils_types" app/\(dashboard\)/layout.tsx
    - grep -q "redirect.*attente\|redirect.*login" app/\(dashboard\)/layout.tsx
    - grep -q "permission" components/layout/RoleGuard.tsx
    - grep -rq "Acces non autorise\|Vous n.avez pas les permissions" components/layout/RoleGuard.tsx
    - grep -q "ouverte" components/ui/status-badge.tsx
    - grep -q "en_cours" components/ui/status-badge.tsx
    - grep -q "bg-green-100" components/ui/status-badge.tsx
    - grep -q "Tableau de bord" app/\(dashboard\)/dashboard/page.tsx
    - npm run build exits 0
  </acceptance_criteria>
  <done>Layout dashboard fonctionnel avec sidebar filtree par permissions, header avec logout, RoleGuard bloquant, StatusBadge avec 5 variants, responsive avec hamburger mobile. Le build compile sans erreur.</done>
</task>

</tasks>

<verification>
- `npm run build` reussit sans erreur
- `npx tsc --noEmit` reussit
- Le middleware redirige les non-authentifies vers /login
- Les pages auth affichent le design BeFast (fond navy, card off-white, CTA gold)
- La sidebar filtre les items selon les permissions
- L'ecran /attente est accessible pour les comptes sans role
- Toute la copy est en francais
</verification>

<success_criteria>
Un utilisateur peut s'inscrire, se connecter, voir l'ecran d'attente si pas de role, acceder au dashboard avec sidebar filtree par role, et etre bloque par RoleGuard sur les pages non autorisees. Le middleware protege toutes les routes dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation/01-02-SUMMARY.md`
</output>
