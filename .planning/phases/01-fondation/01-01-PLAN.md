---
phase: 01-fondation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.js
  - tailwind.config.ts
  - postcss.config.js
  - app/globals.css
  - app/layout.tsx
  - components.json
  - lib/supabase/server.ts
  - lib/supabase/client.ts
  - lib/supabase/admin.ts
  - lib/encryption.ts
  - supabase/migrations/001_init_schema.sql
  - supabase/migrations/002_rls_policies.sql
  - supabase/migrations/003_functions.sql
  - .env.local.example
  - .gitignore
autonomous: true
requirements:
  - AUTH-06
  - AUTH-08
  - UX-01
  - UX-02
  - UX-03
  - SEC-05
  - SEC-06
must_haves:
  truths:
    - "Le projet Next.js 14 compile et demarre sans erreur"
    - "Les CSS variables shadcn/ui utilisent la palette BeFast (#0D1B2A, #C9A84C, #F5F0E8, #4A90D9)"
    - "Les fonts Playfair Display et DM Sans sont chargees via next/font"
    - "Les utilitaires Supabase server/client/admin existent et compilent"
    - "Les migrations SQL definissent profils_types, personnes et les 4 roles par defaut"
    - "ENCRYPTION_KEY n'a pas de prefixe NEXT_PUBLIC_"
  artifacts:
    - path: "app/globals.css"
      provides: "CSS variables shadcn/ui palette BeFast"
      contains: "--primary:        40 56% 54%"
    - path: "app/layout.tsx"
      provides: "Root layout avec fonts fr"
      contains: "lang=\"fr\""
    - path: "lib/supabase/server.ts"
      provides: "createClient server-side"
      exports: ["createClient"]
    - path: "lib/supabase/client.ts"
      provides: "createClient browser-side"
      exports: ["createClient"]
    - path: "lib/supabase/admin.ts"
      provides: "createAdminClient service_role"
      contains: "server-only"
    - path: "lib/encryption.ts"
      provides: "AES-256-GCM encrypt/decrypt"
      exports: ["encrypt", "decrypt", "encryptToString", "decryptFromString"]
    - path: "next.config.js"
      provides: "standalone output"
      contains: "output: 'standalone'"
  key_links:
    - from: "lib/supabase/admin.ts"
      to: "server-only"
      via: "import 'server-only'"
      pattern: "import 'server-only'"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals.css"
---

<objective>
Initialiser le projet Next.js 14 avec le design system BeFast, les utilitaires Supabase SSR, le module de chiffrement et les migrations SQL de base.

Purpose: Fondation technique sur laquelle toutes les pages et fonctionnalites reposent.
Output: Projet compilable avec palette, fonts, clients Supabase, encryption utils et schema SQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/TECH-STACK.md
@.planning/research/UI-PATTERNS.md
@.planning/phases/01-fondation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialiser le projet Next.js 14 + shadcn/ui + design system BeFast</name>
  <files>
    package.json, tsconfig.json, next.config.js, tailwind.config.ts, postcss.config.js,
    app/globals.css, app/layout.tsx, app/page.tsx, components.json, .gitignore, .env.local.example
  </files>
  <read_first>
    .planning/research/UI-PATTERNS.md
    .planning/phases/01-fondation/01-UI-SPEC.md
  </read_first>
  <action>
1. Creer le projet Next.js 14 App Router:
   ```bash
   npx create-next-app@14 . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --use-npm
   ```
   Si le dossier n'est pas vide, utiliser un dossier temp puis copier les fichiers.

2. next.config.js — ajouter output standalone (SEC-06):
   ```js
   /** @type {import('next').NextConfig} */
   const nextConfig = {
     output: 'standalone',
   }
   module.exports = nextConfig
   ```

3. Installer les dependances:
   ```bash
   npm install @supabase/supabase-js @supabase/ssr server-only
   npx shadcn@latest init
   ```
   Lors de shadcn init, choisir: style=default, baseColor=slate, css variables=yes.

4. Installer les composants shadcn necessaires Phase 1:
   ```bash
   npx shadcn@latest add button input label form card separator avatar skeleton sonner
   ```

5. app/globals.css — remplacer les CSS variables par la palette BeFast exacte (depuis UI-PATTERNS.md section 1):
   ```css
   @tailwind base;
   @tailwind components;
   @tailwind utilities;

   @layer base {
     :root {
       --background:     40 33% 94%;
       --foreground:     210 52% 11%;
       --card:           40 33% 96%;
       --card-foreground: 210 52% 11%;
       --popover:        40 33% 96%;
       --popover-foreground: 210 52% 11%;
       --primary:        40 56% 54%;
       --primary-foreground: 210 52% 11%;
       --secondary:      210 52% 11%;
       --secondary-foreground: 40 33% 94%;
       --accent:         211 65% 57%;
       --accent-foreground: 0 0% 100%;
       --muted:          210 20% 88%;
       --muted-foreground: 210 20% 40%;
       --destructive:    0 72% 51%;
       --destructive-foreground: 0 0% 100%;
       --border:         210 20% 82%;
       --input:          210 20% 82%;
       --ring:           211 65% 57%;
       --radius: 0.5rem;
     }
   }
   ```

6. tailwind.config.ts — ajouter fontFamily et aliases directs BeFast:
   ```typescript
   fontFamily: {
     sans:    ["var(--font-dm-sans)", "system-ui", "sans-serif"],
     serif:   ["var(--font-playfair)", "Georgia", "serif"],
     heading: ["var(--font-playfair)", "Georgia", "serif"],
   },
   colors: {
     // ... garder les semantic tokens shadcn ...
     navy:  "#0D1B2A",
     gold:  "#C9A84C",
     ivory: "#F5F0E8",
     blue:  "#4A90D9",
   },
   ```

7. app/layout.tsx — configurer fonts + lang="fr" (UX-01, UX-03):
   ```typescript
   import { Playfair_Display, DM_Sans } from "next/font/google";
   import "./globals.css";
   import { Toaster } from "@/components/ui/sonner";

   const playfair = Playfair_Display({
     subsets: ["latin"],
     variable: "--font-playfair",
     display: "swap",
     weight: ["400", "600", "700"],
   });
   const dmSans = DM_Sans({
     subsets: ["latin"],
     variable: "--font-dm-sans",
     display: "swap",
     weight: ["400", "500", "600"],
   });

   export const metadata = {
     title: "BeFast — Odensia Junior Conseil",
     description: "Plateforme de gestion interne",
   };

   export default function RootLayout({ children }: { children: React.ReactNode }) {
     return (
       <html lang="fr" className={`${playfair.variable} ${dmSans.variable}`}>
         <body className="font-sans bg-background text-foreground antialiased">
           {children}
           <Toaster position="top-right" />
         </body>
       </html>
     );
   }
   ```

8. app/page.tsx — page d'accueil temporaire qui redirige vers /login:
   ```typescript
   import { redirect } from 'next/navigation'
   export default function Home() { redirect('/login') }
   ```

9. .env.local.example (SEC-05 — pas de NEXT_PUBLIC_ pour les cles privees):
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
   SUPABASE_SERVICE_ROLE_KEY=eyJ...
   ENCRYPTION_KEY=64_char_hex_string
   ```

10. .gitignore — s'assurer que .env.local est ignore.
  </action>
  <verify>
    <automated>cd /Users/felixpitz/Desktop/Befast && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "output.*standalone" next.config.js
    - grep -q 'lang="fr"' app/layout.tsx
    - grep -q "\-\-primary:.*40 56% 54%" app/globals.css
    - grep -q "font-playfair" app/layout.tsx
    - grep -q "font-dm-sans" app/layout.tsx
    - grep -q "ENCRYPTION_KEY" .env.local.example
    - ! grep -q "NEXT_PUBLIC_ENCRYPTION_KEY" .env.local.example
    - ! grep -q "NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY" .env.local.example
    - grep -q "navy.*#0D1B2A" tailwind.config.ts
    - grep -q "gold.*#C9A84C" tailwind.config.ts
    - npm run build succeeds (exit 0)
  </acceptance_criteria>
  <done>Le projet compile, la palette BeFast est active, les fonts sont configurees, output standalone est set.</done>
</task>

<task type="auto">
  <name>Task 2: Creer les utilitaires Supabase SSR + admin + encryption</name>
  <files>
    lib/supabase/server.ts, lib/supabase/client.ts, lib/supabase/admin.ts, lib/encryption.ts
  </files>
  <read_first>
    .planning/research/TECH-STACK.md
  </read_first>
  <action>
1. lib/supabase/server.ts — createServerClient depuis @supabase/ssr (AUTH-06 session persistence):
   ```typescript
   import { createServerClient } from '@supabase/ssr'
   import { cookies } from 'next/headers'

   export function createClient() {
     const cookieStore = cookies()
     return createServerClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
       {
         cookies: {
           getAll() { return cookieStore.getAll() },
           setAll(cookiesToSet) {
             try {
               cookiesToSet.forEach(({ name, value, options }) =>
                 cookieStore.set(name, value, options)
               )
             } catch { /* Server Component — middleware handles refresh */ }
           },
         },
       }
     )
   }
   ```

2. lib/supabase/client.ts — createBrowserClient:
   ```typescript
   import { createBrowserClient } from '@supabase/ssr'

   export function createClient() {
     return createBrowserClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
     )
   }
   ```

3. lib/supabase/admin.ts — service_role avec guard server-only (SEC-05):
   ```typescript
   import { createClient } from '@supabase/supabase-js'
   import 'server-only'

   export function createAdminClient() {
     const url = process.env.NEXT_PUBLIC_SUPABASE_URL
     const key = process.env.SUPABASE_SERVICE_ROLE_KEY
     if (!url || !key) throw new Error('Missing Supabase admin credentials')
     return createClient(url, key, {
       auth: { autoRefreshToken: false, persistSession: false },
     })
   }
   ```

4. lib/encryption.ts — AES-256-GCM encrypt/decrypt (ENCRYPTION_KEY sans NEXT_PUBLIC_):
   Copier exactement le code de TECH-STACK.md section 4 (functions encrypt, decrypt, encryptToString, decryptFromString).
   Ajouter `import 'server-only'` en haut du fichier.
  </action>
  <verify>
    <automated>cd /Users/felixpitz/Desktop/Befast && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "createServerClient" lib/supabase/server.ts
    - grep -q "createBrowserClient" lib/supabase/client.ts
    - grep -q "server-only" lib/supabase/admin.ts
    - grep -q "server-only" lib/encryption.ts
    - grep -q "aes-256-gcm" lib/encryption.ts
    - grep -q "export function createAdminClient" lib/supabase/admin.ts
    - grep -q "export function encryptToString" lib/encryption.ts
    - grep -q "export function decryptFromString" lib/encryption.ts
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Les 4 fichiers utilitaires existent, compilent, et server-only protege admin.ts et encryption.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Creer les migrations SQL Supabase (schema, RLS, fonctions)</name>
  <files>
    supabase/migrations/001_init_schema.sql,
    supabase/migrations/002_rls_policies.sql,
    supabase/migrations/003_functions.sql
  </files>
  <read_first>
    .planning/research/TECH-STACK.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
1. supabase/migrations/001_init_schema.sql (ROLE-01 — 4 roles par defaut, AUTH-08 — structure pour disable confirm email):

   ```sql
   -- Table des types de profils (roles) avec permissions JSONB
   CREATE TABLE public.profils_types (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     nom TEXT NOT NULL UNIQUE,
     slug TEXT NOT NULL UNIQUE,
     permissions JSONB NOT NULL DEFAULT '{}'::jsonb,
     est_defaut BOOLEAN NOT NULL DEFAULT false,
     created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
     updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   -- Table des personnes (profils utilisateurs)
   CREATE TABLE public.personnes (
     id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
     email TEXT NOT NULL,
     prenom TEXT,
     nom TEXT,
     portable TEXT,
     promo TEXT,
     adresse TEXT,
     ville TEXT,
     code_postal TEXT,
     pole TEXT,
     nss_encrypted TEXT,
     iban_encrypted TEXT,
     encryption_key_version INTEGER DEFAULT 1,
     profil_type_id UUID REFERENCES public.profils_types(id),
     actif BOOLEAN NOT NULL DEFAULT true,
     created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
     updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
   );

   -- Seed les 4 roles par defaut
   INSERT INTO public.profils_types (nom, slug, permissions, est_defaut) VALUES
     ('Membre AGC', 'membre_agc', '{"dashboard":true,"profil":true,"missions":true,"etudes":true,"documents":true}', true),
     ('Ancien Membre AGC', 'ancien_membre_agc', '{"dashboard":true,"profil":true,"documents":true}', true),
     ('Intervenant', 'intervenant', '{"dashboard":true,"profil":true,"missions":true,"documents":true}', true),
     ('Administrateur', 'administrateur', '{"dashboard":true,"profil":true,"missions":true,"etudes":true,"prospection":true,"statistiques":true,"administration":true,"documents":true}', true);

   -- Trigger pour creer un profil personne quand un user s'inscrit
   CREATE OR REPLACE FUNCTION public.handle_new_user()
   RETURNS trigger LANGUAGE plpgsql SECURITY DEFINER AS $$
   BEGIN
     INSERT INTO public.personnes (id, email, prenom, nom)
     VALUES (
       NEW.id,
       NEW.email,
       NEW.raw_user_meta_data->>'prenom',
       NEW.raw_user_meta_data->>'nom'
     );
     RETURN NEW;
   END;
   $$;

   CREATE TRIGGER on_auth_user_created
     AFTER INSERT ON auth.users
     FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

   -- Trigger updated_at
   CREATE OR REPLACE FUNCTION public.update_updated_at()
   RETURNS trigger LANGUAGE plpgsql AS $$
   BEGIN
     NEW.updated_at = now();
     RETURN NEW;
   END;
   $$;

   CREATE TRIGGER personnes_updated_at BEFORE UPDATE ON public.personnes
     FOR EACH ROW EXECUTE FUNCTION public.update_updated_at();
   CREATE TRIGGER profils_types_updated_at BEFORE UPDATE ON public.profils_types
     FOR EACH ROW EXECUTE FUNCTION public.update_updated_at();
   ```

2. supabase/migrations/002_rls_policies.sql:

   ```sql
   -- Enable RLS
   ALTER TABLE public.personnes ENABLE ROW LEVEL SECURITY;
   ALTER TABLE public.profils_types ENABLE ROW LEVEL SECURITY;

   -- Helper: get user role slug from JWT claims
   CREATE OR REPLACE FUNCTION public.get_my_role()
   RETURNS text LANGUAGE sql STABLE AS $$
     SELECT coalesce(
       current_setting('request.jwt.claims', true)::jsonb->>'app_role',
       null
     )
   $$;

   -- Helper: check permission for a page
   CREATE OR REPLACE FUNCTION public.has_permission(page_name text)
   RETURNS boolean LANGUAGE sql STABLE AS $$
     SELECT coalesce(
       (SELECT (pt.permissions->>page_name)::boolean
        FROM public.personnes p
        JOIN public.profils_types pt ON pt.id = p.profil_type_id
        WHERE p.id = auth.uid()),
       false
     )
   $$;

   -- profils_types: tous les authentifies peuvent lire
   CREATE POLICY "authenticated read profils_types"
     ON public.profils_types FOR SELECT
     TO authenticated
     USING (true);

   -- profils_types: seuls les admins modifient
   CREATE POLICY "admin manage profils_types"
     ON public.profils_types FOR ALL
     TO authenticated
     USING (public.get_my_role() = 'administrateur');

   -- personnes: lire son propre profil
   CREATE POLICY "users read own profile"
     ON public.personnes FOR SELECT
     TO authenticated
     USING (auth.uid() = id);

   -- personnes: admins lisent tous les profils
   CREATE POLICY "admin read all profiles"
     ON public.personnes FOR SELECT
     TO authenticated
     USING (public.get_my_role() = 'administrateur');

   -- personnes: modifier son propre profil
   CREATE POLICY "users update own profile"
     ON public.personnes FOR UPDATE
     TO authenticated
     USING (auth.uid() = id);

   -- personnes: admins modifient tous les profils
   CREATE POLICY "admin update all profiles"
     ON public.personnes FOR UPDATE
     TO authenticated
     USING (public.get_my_role() = 'administrateur');
   ```

3. supabase/migrations/003_functions.sql — Custom access token hook pour embarquer le role dans le JWT:

   ```sql
   -- Custom access token hook: embed role slug in JWT claims
   CREATE OR REPLACE FUNCTION public.custom_access_token_hook(event jsonb)
   RETURNS jsonb LANGUAGE plpgsql AS $$
   DECLARE
     claims jsonb;
     user_role_slug text;
   BEGIN
     SELECT pt.slug INTO user_role_slug
       FROM public.personnes p
       JOIN public.profils_types pt ON pt.id = p.profil_type_id
       WHERE p.id = (event->>'user_id')::uuid;

     claims := event->'claims';

     IF user_role_slug IS NOT NULL THEN
       claims := jsonb_set(claims, '{app_role}', to_jsonb(user_role_slug));
     ELSE
       claims := jsonb_set(claims, '{app_role}', 'null'::jsonb);
     END IF;

     RETURN jsonb_set(event, '{claims}', claims);
   END;
   $$;

   -- Grant to supabase_auth_admin (required for auth hooks)
   GRANT EXECUTE ON FUNCTION public.custom_access_token_hook TO supabase_auth_admin;
   REVOKE EXECUTE ON FUNCTION public.custom_access_token_hook FROM authenticated, anon, public;

   -- Grant usage on tables needed by the hook
   GRANT SELECT ON public.personnes TO supabase_auth_admin;
   GRANT SELECT ON public.profils_types TO supabase_auth_admin;
   ```
  </action>
  <verify>
    <automated>cat /Users/felixpitz/Desktop/Befast/supabase/migrations/001_init_schema.sql | grep -c "CREATE TABLE" && cat /Users/felixpitz/Desktop/Befast/supabase/migrations/002_rls_policies.sql | grep -c "CREATE POLICY" && cat /Users/felixpitz/Desktop/Befast/supabase/migrations/003_functions.sql | grep -c "custom_access_token_hook"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "profils_types" supabase/migrations/001_init_schema.sql
    - grep -q "personnes" supabase/migrations/001_init_schema.sql
    - grep -q "membre_agc" supabase/migrations/001_init_schema.sql
    - grep -q "ancien_membre_agc" supabase/migrations/001_init_schema.sql
    - grep -q "intervenant" supabase/migrations/001_init_schema.sql
    - grep -q "administrateur" supabase/migrations/001_init_schema.sql
    - grep -q "handle_new_user" supabase/migrations/001_init_schema.sql
    - grep -q "ENABLE ROW LEVEL SECURITY" supabase/migrations/002_rls_policies.sql
    - grep -q "get_my_role" supabase/migrations/002_rls_policies.sql
    - grep -q "has_permission" supabase/migrations/002_rls_policies.sql
    - grep -q "custom_access_token_hook" supabase/migrations/003_functions.sql
    - grep -q "supabase_auth_admin" supabase/migrations/003_functions.sql
  </acceptance_criteria>
  <done>3 fichiers SQL existent avec schema complet, RLS activee, 4 roles seedes, et hook JWT.</done>
</task>

</tasks>

<verification>
- `npm run build` reussit sans erreur
- `npx tsc --noEmit` reussit
- Les 3 migrations SQL sont syntaxiquement valides
- La palette BeFast est dans globals.css
- Les fonts Playfair Display + DM Sans sont dans layout.tsx
- output: 'standalone' dans next.config.js
</verification>

<success_criteria>
Le projet Next.js 14 compile avec le design system BeFast complet, les utilitaires Supabase et encryption sont prets, et les migrations SQL sont ecrites pour deploiement.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation/01-01-SUMMARY.md`
</output>
